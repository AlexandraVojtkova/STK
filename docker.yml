spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/STKdatabase
    username: pouzivatel
    password: heslo
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update